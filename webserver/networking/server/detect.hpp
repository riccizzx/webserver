#ifndef DETECT_SSH_INCLUDES
#define DETECT_SSH_INCLUDES

#include <sstream>
#include <iomanip>
#include <cctype>
#include <string>

static std::string escapeHex(const char* data, size_t len);

namespace escape
{	
	//static std::string escapeHex(const char* data, size_t len);

}

#endif
